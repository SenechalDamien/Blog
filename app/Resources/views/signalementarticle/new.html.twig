{% extends 'base.html.twig' %}

{% block body %}
    <h1>Signaler l'article</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Signaler" />
    {{ form_end(form) }}

{% endblock %}
